export function initialize() {
  // application.inject('route', 'userInfo', 'service:user');
  // application.inject('controller', 'user', 'service:user');
}

export default {
  name: 'init',
  initialize
};
