<div class="ui stackable grid">
  <div class="ten wide column">
    {{#if isEditing}}
      <div class="ui raised block segment">
        {{#if editMessage}}
          <div class="ui info message">
            <i class="close icon" {{action 'closeEditMessage'}}></i>
            The README doc accepts HTML formatting. We don't yet have a built-in HTML editor, but in the meantime, we recommend copying and pasting the HTML below into a free online editor, like <a href="https://html-online.com/" target="_blank"> HTML Online.</a> Or just write plain text. That's cool too.
          </div>
          <div class="ui hidden divider"></div>
        {{/if}}
        <div class="ui right-aligned">
          <button class="ui mini teal button" {{action 'saveReadme'}}>Save</button>
          <button class="ui mini button" {{action 'cancelEdit'}}>Cancel</button>
        </div>
        <div class="ui form">
          <div class="field">
            <label>Edit README (HTML):</label>
            {{one-way-textarea rows=30 value=readmeTemp update=(action (mut readmeTemp))}}
          </div>
        </div>
      </div>
    {{else}}
      <div class="ui raised block segment">
        <h1 class="ui header"><strong>README</strong>{{#if userCanEditReadme}}<a class="sub header" href="#" {{action 'openEdit'}}>Edit</a>{{/if}}</h1>
        <div class="ui clearing divider"></div>
        {{#if model.readme}}
          {{{model.readme}}}
        {{else}}
          <p><em>Nothing to see here - You're on your own <i class="frown icon"></i></em></p>
        {{/if}}
      </div>
    {{/if}}
  </div>
  <div class="six wide column">
    <div class="ui center aligned basic segment">
      <h2 class="ui icon header">
        <i class="users icon"></i>
        <div class="content">
          Clausemasters
          <div class="sub header">This Clause was Hand Crafted by</div>
        </div>
      </h2>
      <div class="ui large labels">
        {{#each model.adminUsers as |user|}}
          <a class="ui basic image label">
            {{ember-initials name=user.fullName size=50}}
            {{user.fullName}}
          </a>
        {{/each}}
        {{#each model.canWriteUsers as |user|}}
          <a class="ui basic image label">
            {{ember-initials name=user.fullName size=50}}
            {{user.fullName}}
          </a>
        {{/each}}
      </div>
      <div class="ui divider"></div>
      <p>{{model.metadata.createdBy}} brought this clause into the world on <strong>{{moment-format model.metadata.createdAt "MMMM Do YYYY"}}</strong>. It was lasted updated <strong>{{moment-from-now model.metadata.lastModified}}</strong> by {{model.metadata.lastModifiedBy}}.</p>
    </div>

    {{!-- <div class="ui card">
      <div class="ui top attached label">Info</div>
      <div class="image">
        <img src="http://immixlaw.com/wp-content/uploads/2016/09/JacobShwartz.jpg">
      </div>
      <div class="content">
        <div class="meta">Owner</div>
        Jacob Shwartz
      </div>
      <div class="content">
        <div class="meta">Contributors</div>
        <div class="description">
          <div class="ui large labels">
            <a class="ui basic image label"><img src="http://immixlaw.com/wp-content/uploads/2016/09/JacobShwartz.jpg"> Jacob</a>
            <a class="ui basic image label"><img src="http://immixlaw.com/wp-content/uploads/2016/09/JacobShwartz.jpg"> Jacob</a>
            <a class="ui basic image label"><img src="http://immixlaw.com/wp-content/uploads/2016/09/JacobShwartz.jpg"> Jacob</a>
          </div>
        </div>
      </div>
      <div class="content">
        <div class="meta">Public Tags</div>
        <div class="description">
          <div class="ui small tag labels">
            {{#each model.tags as |tag|}}
            <a class="ui label">{{tag.name}}<i class="delete icon" {{action 'removeTag' tag}}></i></a>
            {{/each}}
          </div>
        </div>
      </div>
      <div class="extra content">
        <form class="ui form">
          <div class="field">
            {{#ui-dropdown class="search selection" allowAdditions=true onShow=(action 'resetTagDropdown') fullTextSearch=true selected=selectedTag onNoResults=(action 'showNewTag') onChange=(action 'handleTagDropdown') as |execute mapper|}}
              <div class="default text">Search or Create Tags</div>
              <i class="dropdown icon"></i>
              <div class="menu">
                {{#each tags as |tag|}}
                  <div class="item" data-value="{{tag.id}}">
                    {{tag.name}}
                  </div>
                {{/each}}
              </div>
            {{/ui-dropdown}}
          </div>
          {{#if newTagButton}}
            <button class="ui mini teal basic button" {{action 'createTag'}}>Create and Add New Tag</button>
          {{else}}
            <button class="ui mini basic button" {{action 'addTag'}}>Add Tag</button>
          {{/if}}
        </form>
      </div>
    </div> --}}

  </div>

</div>
