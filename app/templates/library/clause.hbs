{{clause-modal clause=model menuOptions=sortedMenuArray finalText=finalText updateDropdown=(action "updateDropdown") updateCheckbox=(action "updateCheckbox") onClose=(action "transitionLibrary")}}


{{outlet}}
